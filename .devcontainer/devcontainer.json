{
  "name": "Forgelet Dev Environment",
  "image": "mcr.microsoft.com/devcontainers/go:1-1.22",
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {
      "version": "latest",
      "enableNonRootDocker": "true",
      "moby": "true"
    },
    "ghcr.io/devcontainers/features/node:1": {
      "version": "lts"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "golang.go"
      ]
    }
  },
  "postCreateCommand": "npm install -g pnpm && go mod download && make build || go build -o bin/forgelet ./cmd/forgelet && sudo cp bin/forgelet /usr/local/bin/forgelet && forgelet prerequisites && forgelet up"
}
